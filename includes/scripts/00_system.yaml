  telegramm_start:
    alias: Отчет при старте системы
    sequence:
      - service: notify.telegram_alex
        data:
           message: | 
               MQTT сервер Raspberry Pi 3 B запущен в {{ states('sensor.time_date') }} 
               
  stub:
    sequence:
      - service: homeassistant.update_entity
        entity_id:
         - sensor.last_boot_custom