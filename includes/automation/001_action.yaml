# - alias: 05_usb_cool_auto
#   id: Охлаждающая подставка
#   initial_state: true
#   trigger:
#     - platform: time_pattern
#       minutes: '/5'
#     - platform: state
#       entity_id:
#         - binary_sensor.living_usb_fan
#   action:
#     service_template: >-
#       {% if is_state("binary_sensor.living_usb_fan", "on") %}
#           switch.turn_on
#       {% elif is_state("binary_sensor.living_usb_fan", "off") %}
#           switch.turn_off
#       {%- endif -%}  
#     entity_id: 
#       - switch.sonoff_1000d036a0_1

- id: Режим отпуск
  alias: info_vacancy
  initial_state: true
  trigger:
  - platform: state
    entity_id: binary_sensor.vacancy
  action:
    - service: notify.telegram_alex
      data:
        message: "Изменение режима Отпуск - {{ states('binary_sensor.vacancy') }}.  Время события - {{ states('sensor.time_date') }}"
